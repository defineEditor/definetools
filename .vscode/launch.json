{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/bin/run",
      "args": [
        "getvars",
        "--stdout",        
        "-f=json"
        "data/define.sdtm.invalid.xml"
      ]
    }
  ]
}
